/* import { actionsCondition } from '../reducersFn'
import { dispatchActionsWithApi } from '../actionsFn'

export const reduxActions = (data) => {
  return {
    reducer: actionsCondition(data.reducer),
    action: dispatchActionsWithApi(data.reducer)
  }
} */
